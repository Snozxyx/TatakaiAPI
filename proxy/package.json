{
  "name": "streaming-proxy-server",
  "version": "1.0.0",
  "description": "A Node.js streaming proxy server with caching and HLS support",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "dev:cf": "npx wrangler dev --config wrangler.toml",
    "deploy:cf": "npx wrangler deploy --config wrangler.toml"
  },
  "dependencies": {
    "express": "^4.22.2",
    "node-fetch": "^2.6.7",
    "dotenv": "^16.0.3",
    "node-cache": "^5.1.2",
    "morgan": "^1.11.0",
    "helmet": "^7.0.0",
    "envalid": "^7.3.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
